# Build-time configuration for the deployed site.
#
# Vite inlines anything prefixed VITE_ into the bundle at build time, so this is
# public by definition — never put a secret here.
#
# Origin only: `/api` is appended in src/api/index.js. No trailing slash.
VITE_API_URL=https://api.f2hmarket.com:8443
